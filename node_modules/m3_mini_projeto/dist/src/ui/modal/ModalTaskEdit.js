var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { createHeadingTitle, createSection } from "../dom/index.js";
export function renderModalEditTask(task, user) {
    return __awaiter(this, void 0, void 0, function* () {
        const modal = createSection("modalEditTask");
        modal.classList.add("modal");
        const content = createSection("modalEditTaskContent");
        content.classList.add("modal-content");
        const closeBtn = document.createElement("span");
        closeBtn.classList.add("close");
        closeBtn.innerHTML = "&times;";
        closeBtn.onclick = () => modal.remove();
        const titleHeading = createHeadingTitle("h2", `Editar ${task.getTitle()}`);
        const leftPanel = yield renderEditTaskLeftPanel(task, user, modal);
        const rightPanel = yield renderEditTaskRightPanel(task);
        const mainContainer = document.createElement("div");
        mainContainer.className = "edit-task-main";
        mainContainer.style.display = "flex";
        const divider = document.createElement("div");
        divider.className = "edit-task-divider";
        divider.style.width = "1px";
        divider.style.backgroundColor = "#ccc";
        mainContainer.append(leftPanel.leftContainer, divider, rightPanel);
        content.append(closeBtn, titleHeading, mainContainer);
        modal.append(content);
        document.body.appendChild(modal);
        modal.onclick = (e) => {
            if (e.target === modal)
                modal.remove();
        };
        modal.style.display = "flex";
    });
}
