const express = require("express");

const router = express.Router();

/* Rota para obter informações de todas os sprints */
router.get("/", (req, res) => {

});

/* Rota para obter informações do sprint pelo id */
router.get("/:id", (req, res) => {
  
});

/* Rota para criar uma novo sprint */
router.post("/", (req, res) => {
 
});

/* Rota para atualizar informações do sprint */
router.put("/:id", (req, res) => {
  
});

/* Rota para excluir um sprint */
router.delete("/:id", (req, res) => {

});

module.exports = router;
