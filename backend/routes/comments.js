const express = require("express");

const router = express.Router();

/* Rota para obter informações de todas os comentarios */
router.get("/", (req, res) => {

});

/* Rota para obter informações do comentario pelo id */
router.get("/:id", (req, res) => {
  
});

/* Rota para criar uma novo comentario */
router.post("/", (req, res) => {
 
});

/* Rota para atualizar informações do comentario */
router.put("/:id", (req, res) => {
  
});

/* Rota para excluir um comentario */
router.delete("/:id", (req, res) => {

});

module.exports = router;
