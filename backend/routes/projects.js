const express = require("express");

const router = express.Router();

/* Rota para obter informações de todas os projetos */
router.get("/", (req, res) => {

});

/* Rota para obter informações do projeto pelo id */
router.get("/:id", (req, res) => {
  
});

/* Rota para criar uma novo projeto */
router.post("/", (req, res) => {
 
});

/* Rota para atualizar informações do projeto */
router.put("/:id", (req, res) => {
  
});

/* Rota para excluir um projeto */
router.delete("/:id", (req, res) => {

});

module.exports = router;
